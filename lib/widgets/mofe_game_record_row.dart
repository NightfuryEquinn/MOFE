import 'package:flutter/material.dart';

class MofeGameRecordRow extends StatelessWidget {
  const MofeGameRecordRow({super.key});

  @override
  Widget build(BuildContext context) {
    return const Row(
      children: [

      ],
    );
  }
}